Introduction
============

Adds a "sticky" checkbox to news items and events so they can be marked as sticky.
Sticky items will appear at the top of collections that use the sticky_folder_listing layout.
